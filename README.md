
  # UI/UX Handoff Document

  This is a code bundle for UI/UX Handoff Document. The original project is available at https://www.figma.com/design/9NDpPjbMXYEcZA7FmqnrlY/UI-UX-Handoff-Document.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  